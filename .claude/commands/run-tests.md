# Run Validation Tests

Run the full 5-scenario validation suite against the reference PDF data.

```bash
cd /Users/consultadd/Downloads/Thunderbird-SolarPumps/solar_pump_calculator && .venv/bin/python test_validation_scenarios.py
```

All 5 scenarios must PASS before sharing with the client or deploying.
After running, summarize which scenarios passed and which failed, and explain any failures concisely.
